---
name: intake
description: Close readiness check that opens a close. Use when starting a close for a period, when the user asks whether books are ready to close, or when validating a trial balance and GL export against statements.
argument-hint: "[client and period, or whatever files you have on hand]"
---

# /intake

Load `${CLAUDE_PLUGIN_ROOT}/skills/financial-close/intake/SKILL.md` and follow it end-to-end.

Plugin root: ${CLAUDE_PLUGIN_ROOT}. Wherever a skill or agent file says `<plugin root>`, it
means this folder. Pass it to any plugin agent you spawn.

Input for this run: $ARGUMENTS
