---
name: close
description: Run a full accounting close for one client and one period, from readiness check to delivered close package.
argument-hint: "<client> <period>"
---

# /close

Load `${CLAUDE_PLUGIN_ROOT}/skills/financial-close/close/SKILL.md` and follow it end-to-end.

Plugin root: ${CLAUDE_PLUGIN_ROOT}. Wherever a skill or agent file says `<plugin root>`, it
means this folder. Pass it to any plugin agent you spawn.

Input for this run: $ARGUMENTS
