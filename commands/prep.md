---
name: prep
description: The prep phase of a close, reconciliation then transaction categorization. Use when prepping a period for close, or when the user wants reconciliation and categorization run together.
argument-hint: "[client and period]"
---

# /prep

Load `${CLAUDE_PLUGIN_ROOT}/skills/prep/SKILL.md` and follow it end-to-end.

Plugin root: ${CLAUDE_PLUGIN_ROOT}. Wherever a skill or agent file says `<plugin root>`, it
means this folder. Pass it to any plugin agent you spawn.

Input for this run: $ARGUMENTS
