---
name: categorize
description: Transaction review and cleanup for a period. Use when sweeping uncategorized or miscoded transactions, matching transfers between accounts, or hunting duplicates before a close.
argument-hint: "[client and period]"
---

# /categorize

Load `${CLAUDE_PLUGIN_ROOT}/skills/financial-close/categorize/SKILL.md` and follow it end-to-end.

Plugin root: ${CLAUDE_PLUGIN_ROOT}. Wherever a skill or agent file says `<plugin root>`, it
means this folder. Pass it to any plugin agent you spawn.

Input for this run: $ARGUMENTS
