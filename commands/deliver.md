---
name: deliver
description: Assemble the close package. Use when producing final statements, a close memo, a workpapers workbook, or an executive summary after review returns ready.
argument-hint: "[client and period]"
---

# /deliver

Load `${CLAUDE_PLUGIN_ROOT}/skills/deliver/SKILL.md` and follow it end-to-end.

Plugin root: ${CLAUDE_PLUGIN_ROOT}. Wherever a skill or agent file says `<plugin root>`, it
means this folder. Pass it to any plugin agent you spawn.

Input for this run: $ARGUMENTS
